export const SOLANA_TRADE_PROMPT = `Swap tokens via Jupiter.

Inputs (all optional):
- inputMint
- outputMint
- inputAmount
- slippageBps (e.g., 100 = 1%)

The UI lets the user edit any missing fields before swapping.`;
