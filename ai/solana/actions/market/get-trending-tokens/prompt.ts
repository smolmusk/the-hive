export const SOLANA_GET_TRENDING_TOKENS_PROMPT = `Fetch trending tokens on Solana (Birdeye).

Input:
- limit (optional)

After the tool returns, do not list tokens in text. Ask what the user wants to do next.` as const; 
